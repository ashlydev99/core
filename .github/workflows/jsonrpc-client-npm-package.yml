name: "Publish @nexuschat/jsonrpc-client"
on:
  workflow_dispatch:
  release:
    types: [published]

permissions: {}

jobs:
  pack-module:
    name: "Publish @nexuschat/jsonrpc-client"
    runs-on: ubuntu-latest
    environment:
      name: npm-jsonrpc-client
      url: https://www.npmjs.com/package/@nexuschat/jsonrpc-client
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          show-progress: false
          persist-credentials: false

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      # Ensure npm 11.5.1 or later is installed.
      # It is needed for <https://docs.npmjs.com/trusted-publishers>
      - name: Update npm
        run: npm install -g npm@latest

      - name: Install dependencies without running scripts
        working-directory: nexuschat-jsonrpc/typescript
        run: npm install --ignore-scripts

      - name: Package
        working-directory: nexuschat-jsonrpc/typescript
        run: |
          npm run build
          npm pack .

      - name: Publish
        working-directory: nexuschat-jsonrpc/typescript
        run: npm publish --provenance nexuschat-jsonrpc-client-* --access public
